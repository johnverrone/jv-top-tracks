import { MetaFunction } from 'remix';

export const meta: MetaFunction = () => {
  return {
    title: "jv's bops | tracks",
    description: "jv's top tracks",
  };
};

export default function Tracks() {
  return <div className="wrapper">coming soon...</div>;
}
